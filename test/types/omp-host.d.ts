// Supplied by OMP's native loader, not installed or modeled as Pi's SDK.
// Consumers must guard the imported value and each member they invoke.
declare module "@oh-my-pi/pi-coding-agent" {
  const host: unknown;
  export = host;
}
